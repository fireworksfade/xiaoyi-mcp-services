# MCP 数据库迁移快照测试说明

迁移测试以程序化方式构造数据库形态，而不是提交二进制 SQLite 文件：

- `empty`：全新空库（对应全新部署）。
- `legacy`：手建旧形态表 + 插入历史行（缺 `claimed_at`/`document_id`/观测列等），
  等价于"当前发布版 Diagnosis SQLite"，验证迁移补列与数据保全。
- `behind/ahead/checksum`：版本表被改写或脚本被篡改，验证拒绝启动。

如需保存真实生产快照，请将 `.db` 文件放入本目录并在测试中复制到临时目录使用；
提交前必须脱敏。
